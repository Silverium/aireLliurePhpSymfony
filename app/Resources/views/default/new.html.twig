{% extends 'base.html.twig' %}
{% block title %}
    Crear competitiono
{% endblock %}
{% block body %}
<div id="competitionCreator">
    <h2>Formulario que será para crear competitionos</h2>
{{ form_start(form, {'attr': {'class': 'form'}}) }}
    {{ form_errors(form) }}
<div class="form-group">
    {{ form_row(form.title) }}</div>
    <div class="form-group ">{{ form_row(form.place) }}</div>
{{ form_end(form) }}
</div>
    <div id="competitionsInscriptable">
    {% for competition in competitions %}
        <div class="activitats">
            <div class='bigtext'>
                <b>{{ competition.title }}</b>
            </div>
            <div>
                <p class="pull-left">{{ competition.date|date('Y-m-d') }}</p>
                <p class="pull-right">{{ competition.place }}</p>
            </div>
            <p class="espacioVer">
                <a href='{{ competition.inscrLink  }}' target="_blank">
                    <img alt="" class="recuadro" src='{{ competition.imgLink }}' width="100%" /></a></p>
            <div>
                <p class="pull-left">{{ competition.organizer }}</p>
                <p class="pull-right">Estat:
                    <span class="red" style="font-weight: bold">{% if competition.inscriptable %} Inscripcions Obertes {% else %}Inscripcions tancades {% endif %}</span></p>
            </div>
        </div>
       <p>{{ competition.title }}</p>
        <p>{{ competition.place }}</p>
        {% endfor %}
    </div>
{% endblock %}